<h2>List</h2>
<div class="container">
    {{#each model as |message|}}
        {{msg-item msg=message}}
    {{/each}}
</div>

{{infinity-loader infinityModel=model loadingText="Loading..." loadedText=""}}