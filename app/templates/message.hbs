<h1>{{model.title}}</h1>
<p>{{model.content}}</p>

{{#if model.comments}}
    <h3>Comments</h3>
    {{#each model.comments as |comment|}}
        <p>{{comment.comment}}</p>
        <hr />
    {{/each}}
{{/if}}

{{outlet}}
